apiVersion: req.nephio.org/v1alpha1
kind: Interface
metadata:
  name: n2
  annotations:
    # N2 carries NGAP signalling between CU-CP and the AMF (5G core).
    nephio.org/network-name: n2
    config.kubernetes.io/local-config: "true"
    specializer.nephio.org/owner: workload.nephio.org/v1alpha1.NFDeployment.gnb-example
    specializer.nephio.org/namespace: example
spec:
  networkInstance:
    name: vpc-ran
  cniType: macvlan
  attachmentType: vlan
