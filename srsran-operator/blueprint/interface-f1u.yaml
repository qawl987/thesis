apiVersion: req.nephio.org/v1alpha1
kind: Interface
metadata:
  name: f1u
  annotations:
    # F1U carries GTP-U user-plane traffic between CU-UP and DU.
    nephio.org/network-name: f1u
    config.kubernetes.io/local-config: "true"
    specializer.nephio.org/owner: workload.nephio.org/v1alpha1.NFDeployment.gnb-example
    specializer.nephio.org/namespace: example
spec:
  networkInstance:
    name: vpc-internal
  cniType: macvlan
  attachmentType: vlan
